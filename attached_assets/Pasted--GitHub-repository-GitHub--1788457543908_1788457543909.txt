اتصال GitHub تأیید شد و آماده است. حالا نام کاربری و repositoryهای قابل‌دسترسی را از GitHub می‌خوانم، سپس repository مقصد را پیدا می‌کنم و commit آماده‌ی نغمه را push می‌کنم.اتصال GitHub آماده است. قبل از نوشتن چیزی، remote فعلی پروژه و هویت GitHub را بررسی می‌کنم تا repository اشتباه انتخاب نشود؛ سپس فقط همین commit پایدار را به repository مقصد می‌فرستم.نام کاربری GitHub از اولین روش API در این محیط در دسترس نشد، اما اتصال معتبر است و مسیر پشتیبانی‌شده‌ی proxy را دارد. با همان اتصال، اطلاعات حساب و repositoryهای قابل‌دسترسی را می‌خوانم و ادامه می‌دهم؛ هیچ credentialی نمایش داده نمی‌شود.حساب GitHub با موفقیت وصل است، اما فقط یک repository در دسترس پیدا شد:ayanokoujiiii/turtle-keeperاین نام با پروژه‌ی نغمه هم‌خوانی ندارد و برای جلوگیری از فرستادن کد به مقصد اشتباه، هنوز چیزی push نکردم. می‌توانم یک repository جدید با نام naghme بسازم یا از همین repository استفاده کنم؛ انتخاب مقصد روی push و مالکیت کد اثر مستقیم دارد.مقصد GitHubساخت repository جدید با نام naghme
برای ساخت repository جدید، فقط یک تنظیم مهم باقی مانده: عمومی باشد یا خصوصی. چون پروژه شامل کد و تنظیمات شخصی است، حالت خصوصی امن‌تر است و فقط خودت/همکارانی که اضافه می‌کنی آن را می‌بینند. قبل از ساخت، این گزینه را انتخاب کن.نوع دسترسی repositoryعمومی
repository عمومی انتخاب شد. حالا repository naghme را در حساب GitHub می‌سازم و سپس محتوای commit پایدار 022efef را با API رسمی GitHub منتقل می‌کنم؛ چون credential در اختیار terminal نیست، push را امن و مستقیم از طریق اتصال متصل‌شده انجام می‌دهم.repository ساخته شد. برای انتقال کامل، از GitHub Git Data API استفاده می‌کنم تا یک commit واحد با همان ساختار پروژه ایجاد شود (نه ۲۰۱ commit جداگانه). ابتدا مسیر خواندن فایل‌های workspace را با یک فایل کوچک تست می‌کنم، بعد همه‌ی فایل‌های tracked را منتقل می‌کنم.اولین تست ارسال blob با پاسخ 409 برگشت؛ این خطا credential نیست و احتمالاً از وضعیت repository خالی یا مسیر Git Data API است. متن خطای GitHub را می‌خوانم و قبل از ادامه روش سازگارتر را انتخاب می‌کنم.خطای GitHub مشخص شد: repository خالی است و GitHub تا وقتی اولین commit ساخته نشود، endpoint ساخت blob را قبول نمی‌کند. یک commit اولیه‌ی فنی با .gitignore واقعی پروژه می‌سازم، سپس commit milestone اصلی را به‌صورت یکپارچه روی آن قرار می‌دهم؛ فایل نهایی پروژه همان commit پایدار خواهد بود.اولین commit فنی روی GitHub ساخته شد و حالا endpoint blob فعال است. ۲۰۱ فایل tracked پروژه را به‌صورت blobهای Git، با حفظ نام مسیرها و بدون نمایش محتوای حساس، منتقل می‌کنم تا در گام بعد یک commit واحد و شاخه‌ی main ساخته شود.