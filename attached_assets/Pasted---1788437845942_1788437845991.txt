 فاز ۱۴ را می‌خوانم و دستورهای اجرایی‌اش را با ساختار فعلی نغمه تطبیق می‌دهم؛ بعد تغییرات را مستقیم روی اپ اعمال و نتیجه را بررسی می‌کنم.

فاز ۱۴ مشخص است: تمرکز روی استودیوی کارت‌پستال است—حرکت واقعی متن و استیکرها، خروجی گالری، بلور، منوهای تو‌در‌تو، ابعاد سفارشی و فونت‌های واقعی. حالا فایل‌های اصلی استودیو و وابستگی‌های فعلی را بررسی می‌کنم تا تغییرات بدون شکستن قابلیت‌های قبلی اعمال شوند.

برای اینکه فونت‌ها واقعاً روی دستگاه کار کنند، بسته‌های فونت فارسی/عربی را با نسخه‌های سازگار با Expo اضافه می‌کنم. هم‌زمان ساختار استودیو را طوری بازطراحی می‌کنم که وضعیت هر استیکر مستقل بماند و منوها شلوغ نشوند.

هسته‌ی قابلیت‌ها اعمال شد: بوم خروجی حالا یک wrapper غیرقابل‌فشرده‌سازی دارد، حرکت‌ها با Pan/Pinch مستقل شده‌اند، تنظیمات استیکر به همان استیکر وصل است و پنل‌های رنگ/فونت/پس‌زمینه تفکیک شده‌اند. حالا typecheck و لاگ‌های اجرای Expo را بررسی می‌کنم تا خطاهای نسخه‌ای یا تایپی را قبل از تحویل رفع کنم.

بررسی تایپی بدون خطا گذشت و Expo هم در حال اجراست. یک پیش‌نمایش واقعی از اپ می‌گیرم تا مطمئن شوم استودیو در وب‌پیش‌نمایش سفید یا شکسته نیست؛ سپس اگر لازم باشد یک اصلاح نهایی کوچک انجام می‌دهم.

تمام بررسی‌های تایپی پروژه با موفقیت گذشت و باندل وب Expo هم بدون خطای Metro ساخته شد. چون حرکت و ذخیره‌سازی وابسته به اجرای واقعی روی اندروید هستند، یک بررسی تکمیلیِ کوچک برای تست دستگاه پیشنهاد می‌کنم؛ خود پیاده‌سازی فاز ۱۴ کامل است.

فاز ۱۴ استودیو نغمه انجام شد:

متن شعر و استیکرها با Pan و Pinch واقعاً قابل جابه‌جایی و تغییر اندازه هستند.
استیکر انتخاب‌شده منوی تنظیمات شفافیت و گردی گوشه‌ها دارد.
مشکل ذخیره‌سازی اصلاح شد؛ مجوز گالری قبل از capture درخواست می‌شود و لاگ‌های خطای دقیق اضافه شده‌اند.
بوم خروجی با wrapper مناسب برای captureRef آماده شده است.
منوهای متن و پس‌زمینه به زیربخش‌های مرتب تقسیم شدند.
پالت ۱۳ رنگ آماده و دکمه «سفارشی» با Color Wheel واقعی اضافه شد.
بلور پس‌زمینه با اسلایدر جدید به‌صورت زنده تغییر می‌کند.
ابعاد سفارشی با ورودی عرض و ارتفاع اضافه شد.
۱۰ فونت واقعی Google Fonts نصب، بارگذاری و در منوی فونت قابل انتخاب شدند.
typecheck کامل پروژه و باندل Expo بدون خطا انجام شد.
نسخه‌ی به‌روزشده‌ی اپ هم در پیش‌نمایش قرار گرفت و آماده‌ی انتشار است.

n-gmh-or-arshyw-mwsyqy-shkh-sy--atomesusawvcnjw
کد url manually برای تست تو expo رو همیشه بده بعد پایان کار فقط کد expo نه لینک وب و...

حتماً؛ این ترجیح را برای دفعات بعد ذخیره می‌کنم تا بعد از پایان کار، فقط آدرس دستی Expo را بدهم و لینک وب یا موارد دیگر را ارسال نکنم.

ثبت شد. از این به بعد بعد از پایان هر کار فقط آدرس دستی Expo را برای تست می‌دهم.

آدرس Expo فعلی:

exp://c6982d99-79ed-4ad9-aa54-8007f5baf022-00-e2dr46l8o7zv.expo.reed.replit.dev

